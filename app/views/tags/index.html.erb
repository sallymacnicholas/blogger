<h1>All The Tags That Have Been Used</h1>


<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag %>
      <%= link_to 'delete', tag, method: :delete, data: {confirm: "Yo bro, you sure you want to delete this forever?!"} %>
    </li>
  <% end %>
</ul>

<%= link_to 'Back To Home', articles_path %>
